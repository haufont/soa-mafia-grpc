syntax = "proto3";

package role;

option go_package = "mafia-grpc/internal/role";

enum Role {
    UNKNOWN = 0;

    // Town-Aligned Roles
    TOWNIE = 1;
    COMMISAR = 2;

    // Mafia-Aligned Roles
    MAFIA = 100;
}
